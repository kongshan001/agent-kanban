# v0.4 迭代记录

## 日期: 2026-05-02

## 本轮核心: 真实 Agent 接入验证

### ✅ 已完成

#### 1. Hermes Agent 接入
- `evaluator.py`: 新增 hermes CLI 调用路径（优先 claude，fallback hermes）
- `runner.py`: hermes 作为 openclaw 类型的实际执行器
- `agent detect`: 检测到 claude-code、opencode、hermes 三个可用 CLI

#### 2. 端到端真实验证
- 用 hermes 完成了规划阶段（生成 requirements.md + task-breakdown.md）
- 用 hermes 完成了执行阶段（实现 multiply/divide + 测试 + 复盘）
- 7 个测试全部通过
- 用 hermes 完成了评估阶段，**真实评分有区分度**：
  - tech_lead: 9.0 ✅（代码质量良好，架构合理）
  - qa: 6.0 ❌（测试覆盖不够）
  - product: 8.5 ❌（需求未完全满足）
  - design: 9.0 ✅（不涉及 UI）

#### 3. 状态机修复
- USER_REVIEW → MERGED → ARCHIVED 闭环修复
- approve 自动合并 + 归档
- reject 支持反馈

### 🐛 发现的问题

#### P0
1. **Hermes 输出清理不彻底**: 报告文件里混入了 prompt 模板和 TUI 装饰符
   - 需要改进 `_clean_hermes_output`，只保留 `## 评估报告` 之后的内容
2. **评分提取不准**: hermes 返回的评分（如 6.0/10）需要更鲁棒的提取

#### P1
3. **4 角色评估太慢**: 每个角色需要单独调用 hermes（~30s/角色），4 个角色要 2 分钟
   - 建议合并为单次评估调用
4. **Claude CLI 无 API key**: 本地 claude CLI 未配置认证，无法使用

### 对照原始需求完成度

| 需求 | 状态 | 验证结果 |
|------|------|---------|
| 1. 看板+分配 | ✅ | hermes 成功执行规划和开发 |
| 2. Git worktree 并行 | ✅ | 3 任务并行验证 |
| 3.1 Plan | ✅ | hermes 生成专业需求文档 |
| 3.2 Execute | ✅ | hermes 实现 2 个函数 + 5 个测试，全部通过 |
| 3.3 Evaluate | 🟡 | hermes 给出真实评分，但输出清理需改进 |
| 3.4 Self-improve | 🟡 | 逻辑完成，需真实 LLM 评估驱动迭代 |
| 3.5 User Decision | ✅ | approve/reject 可用 |
| 3.6 Archive | ✅ | 自动 merge + archive |
| 4.1 减少人参与 | 🟡 | hermes 可自动规划和开发，评估需改进 |
| 4.3 流程强约束 | ✅ | PhaseGuard 正常工作 |

### v0.5 计划
1. 改进 hermes 输出解析（只保留评估内容，去掉模板）
2. 合并 4 角色评估为单次 LLM 调用
3. `kanban task auto` 一键推进命令
4. 配置 claude CLI API key
